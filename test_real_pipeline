#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

if command -v uv >/dev/null 2>&1; then
  exec uv run python scripts/test_real_pipeline.py "$@"
fi

exec python scripts/test_real_pipeline.py "$@"
